import React, { Component } from 'react';

import { View, Text } from 'react-native';

export default class ConfigScreen extends Component {
  render() {
    return <Text>Tela de configuração</Text>;
  }
}
